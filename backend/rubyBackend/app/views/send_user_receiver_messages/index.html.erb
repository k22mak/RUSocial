<h1>Listing send_user_receiver_messages</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Receiver</th>
    <th>Message</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @send_user_receiver_messages.each do |send_user_receiver_message| %>
  <tr>
    <td><%= send_user_receiver_message.username %></td>
    <td><%= send_user_receiver_message.receiver %></td>
    <td><%= send_user_receiver_message.message %></td>
    <td><%= link_to 'Show', send_user_receiver_message %></td>
    <td><%= link_to 'Edit', edit_send_user_receiver_message_path(send_user_receiver_message) %></td>
    <td><%= link_to 'Destroy', send_user_receiver_message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Send user receiver message', new_send_user_receiver_message_path %>
